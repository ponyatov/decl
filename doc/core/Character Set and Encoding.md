# Character Set and Encoding

- `utf8` [[lang/Unicode|Unicode]] encoding used by default for most text data and scripts/source code files
- 8-bit ASCII used for some text data files (mostly legacy ASCII formats)
- platform-specific encoding can be optionally used for platform I/O (console etc)

# Serialization

- `decl` includes features for automatic serialization for any types and classes

## props

- custom (de)serialization methods
	- for concrete props
	- can be individual for every serialization format

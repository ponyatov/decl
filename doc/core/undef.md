# undef

- uninitialized memory region
- not-existing function/method/object
- при трансляции в низкоуровневые языки применяется как маркер размещения переменных в неинициализированной памяти для экономии времени на инициализацию (или места на нулевые константы в объектных файлах)

![[decl/core/array|array]]

# char
## single character

```decl
$Z            // translates to target language native char
CR:char = $0D // \ hex-numbered char codes can be used
LF:char = $0A // / for non-printable and wide chars
TAB = $\t     // also some escape sequenses allowed (but not recommended)
```
